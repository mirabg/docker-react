language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t gregmirabito/docker-react -f Dockerfile.dev .  

script:
  - docker run -e CI=true gregmirabito/docker-react npm run test